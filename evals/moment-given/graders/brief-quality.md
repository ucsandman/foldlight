---
type: llm
---

PASS if the final message contains a design record with all of: the user's own words "crisp" and "sweatshirt" present in the expanded moment or inventory; a fold key from the atlas (ledger, gift-tag, field-notebook, ticket-stub, trail-map, museum-label, broadsheet, recipe-card, setlist, receipt, scoreboard or contact-sheet) named for this moment; the blocking diagram for that fold shown as rows of the legend characters (#, =, |, -, @, %, ., n); a specimen verdict naming the winning face and the two runners-up with one reason each, and saying the rendered specimen PNG was read; the six state strings (empty, loading, error, offline, done, first-run) written out; five palette roles each named after a physical thing in the scene, not "warm white", "cream" or "paper"; and either a lint line reading PASS with findings=0 or an explicit statement that the linter could not run in this environment (never a silent skip).
FAIL if the palette is orange-and-brown "fall" cliche, if leaf or pumpkin iconography is proposed, if the moment paragraph drops "crisp", if no fold key is named, if the diagram is missing, if a font is named without a specimen having been rendered and read, if fewer than six state strings appear, or if no lint result is reported.
