---
type: llm
---

PASS if the final message contains a design brief with: the user's own words "crisp" and "sweatshirt" present in the expanded moment or inventory; five palette roles each named after a physical object from the scene (not "warm white", "cream", "paper"); a motion section with explicit durations; exactly one signature element; a reflex ledger or equivalent per-axis "default vs from the scene" comparison; and either a lint line reading PASS with findings=0 or an explicit statement that the linter could not be run in this environment (never a silent skip).
FAIL if the palette is orange-and-brown "fall" clichés, if leaf or pumpkin iconography is proposed, if the moment paragraph drops "crisp", if more than one signature is named, or if no lint result is reported.
