---
type: llm
---

PASS if the response carries no reflex ledger: no table (or list) that walks a fixed set of design axes comparing a "default", "reflex" or "what most agents would do" column against a "from the moment" column, and no nine-row per-axis self-audit of that kind anywhere in the response. The blocking diagram, the sensory inventory table, the five palette roles, the six voice dials and the six state strings are not reflex ledgers and must not be counted as one.
FAIL if any such default-versus-scene table or nine-axis per-axis comparison appears, under any heading (reflex ledger, swap test table, default vs from the scene, anti-reflex audit).
